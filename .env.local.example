# Amapola Menorca — variables de entorno

# (Opcional) Webhook al que se reenvían los leads de los formularios de
# contacto y B2B. Puede ser un endpoint de email transaccional, Slack, n8n,
# Make/Zapier, etc. Si no se define, los leads solo se registran en el log.
# LEADS_WEBHOOK_URL=https://hooks.example.com/amapola-leads
